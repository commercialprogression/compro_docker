mysql:
  image: mysql:5.6
  environment:
    MYSQL_ROOT_PASSWORD: root
    MYSQL_DATABASE: drupal
  ports:
    - "3306:3306"

web:
  build: .
  ports:
   - "80:80"
  volumes:
   - .:/var/www/html
  links:
   - mysql
