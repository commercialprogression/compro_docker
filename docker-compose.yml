version: '2'
services:
  mysql:
    image: mysql:5.6
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: drupal
    ports:
      - "3307:3306"

  web:
    build: .
    ports:
     - "8000:80"
    volumes:
     - .:/var/www/html
    depends_on:
     - mysql
